% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_fit.R
\name{sc_fit}
\alias{sc_fit}
\title{Fit a synthetic control for one specification}
\usage{
sc_fit(spec, method = "regression", custom_v = NULL, ...)
}
\arguments{
\item{spec}{An \code{sc_spec}.}

\item{method}{One of \code{"regression"} (default), \code{"nested"}, \code{"allopt"},
\code{"custom"}.}

\item{custom_v}{Length-\code{K} non-negative weights. Required for
\code{method = "custom"}.}

\item{...}{Passed to the \(V\) solver (\code{start_seed}, and the inner
QP arguments \code{margin}, \code{maxiter}, \code{sigf}, \code{bound}).}
}
\value{
An object of class \code{scm_fit}: a list with \code{spec}, \code{method},
\code{V}, \code{W}, \code{y_treated}, \code{y_synth}, \code{effect}, \code{att}, \code{rmse}, \code{r2},
\code{pre_mspe}, \code{post_mspe}, \code{mspe_ratio}, and \code{balance}.
}
\description{
Solves predictor weights \eqn{V} and donor weights \eqn{W}, then
builds treated/synthetic paths, ATT, pre-treatment fit, MSPE
ratio, and a predictor balance table.
}
\examples{
spec <- sc_spec(
  cigsale ~ lnincome + cigsale(1988),
  data = smoking, unit = "state", time = "year",
  treat = 3, trperiod = 1989, xperiod = 1980:1988
)
fit <- sc_fit(spec, method = "regression")
fit$att

}
