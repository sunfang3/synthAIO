% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_result.R
\name{scm_fit-extractors}
\alias{scm_fit-extractors}
\alias{coef.scm_fit}
\alias{v_weights}
\alias{balance}
\alias{sc_effects}
\alias{effects}
\alias{glance}
\title{Extract pieces of an \code{scm_fit}}
\usage{
\method{coef}{scm_fit}(object, ...)

v_weights(x, ...)

balance(x, ...)

sc_effects(x, ...)

effects(x, ...)

glance(x, ...)
}
\arguments{
\item{object}{An \code{scm_fit}.}

\item{...}{Unused.}

\item{x}{An \code{scm_fit}.}
}
\value{
\itemize{
\item \code{coef()}: donor weights \(W\) (names are donor ids).
\item \code{v_weights()}: predictor weights \(V\).
\item \code{balance()}: the predictor balance data frame.
\item \code{effects()}: the treated-minus-synthetic gap series.
\item \code{glance()}: one-row data frame of scalars (\code{treated}, \code{trperiod},
\code{method}, \code{n_donors}, \code{att}, \code{rmse}, \code{r2}, \code{pre_mspe}, \code{post_mspe},
\code{mspe_ratio}).
}
}
\description{
These helpers return stored fields. They do not refit or recompute
estimates. \code{glance()} is defined here (no \strong{broom} dependency).
}
\examples{
spec <- sc_spec(
  cigsale ~ lnincome + cigsale(1988),
  data = smoking, unit = "state", time = "year",
  treat = 3, trperiod = 1989, xperiod = 1980:1988
)
fit <- sc_fit(spec, method = "regression")
coef(fit)
v_weights(fit)
glance(fit)

}
