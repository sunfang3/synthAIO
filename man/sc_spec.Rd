% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_spec.R
\name{sc_spec}
\alias{sc_spec}
\title{Parse a long panel plus a synth2-like formula into SCM matrices}
\usage{
sc_spec(
  formula,
  data,
  unit,
  time,
  treat,
  trperiod,
  counit = NULL,
  xperiod = NULL,
  mspeperiod = NULL,
  preperiod = NULL,
  postperiod = NULL
)
}
\arguments{
\item{formula}{A formula. The left-hand side is the outcome. Right-hand
side terms are bare covariate names or \code{var(times)} selectors.}

\item{data}{A long panel data frame.}

\item{unit}{Column name of the unit id (string or unquoted name).}

\item{time}{Column name of the time index (string or unquoted name).}

\item{treat}{Treated unit id, matching \code{data[[unit]]}.}

\item{trperiod}{Treatment-period time point.}

\item{counit}{Optional vector of donor unit ids. The treated unit, if
listed, is dropped with a warning.}

\item{xperiod}{Times used to average bare covariates. Default: all times
\verb{< trperiod}.}

\item{mspeperiod}{Times used for the MSPE / nested objective. Default:
all times \verb{< trperiod}, independent of \code{xperiod}.}

\item{preperiod}{Pre-treatment window (RMSE, \(R^2\)). Default: all times
\verb{< trperiod}.}

\item{postperiod}{Post-treatment window (ATT, post MSPE). Default: all
times \verb{>= trperiod}.}
}
\value{
An object of class \code{sc_spec}: a list with matrices \code{X0}, \code{X1},
\code{Y0}, \code{Y1}, \code{X0_scaled}, \code{X1_scaled}; window vectors \code{donors}, \code{times},
\code{pre_times}, \code{post_times}, \code{mspe_times}, \code{x_times}; slots \code{outcome},
\code{treat}, \code{trperiod}; and the constructor inputs so later steps can
rebuild a spec.
}
\description{
Turns a long unit-time data frame and a formula into the predictor and
outcome matrices an estimator can consume. Bare covariates are averaged
over \code{xperiod}. Calls such as \code{cigsale(1988)}, \code{cigsale(1980:1988)}, and
\code{cigsale(c(1982, 1986, 1988))} select those times and override \code{xperiod}
for that predictor only.
}
\details{
Window arguments default independently: \code{preperiod}, \code{xperiod}, and
\code{mspeperiod} each default to all times \verb{< trperiod}; \code{postperiod} defaults
to all times \verb{>= trperiod}. Setting \code{xperiod} does \strong{not} change
\code{mspeperiod}.
}
\examples{
spec <- sc_spec(
  cigsale ~ lnincome + cigsale(1988),
  data = smoking, unit = "state", time = "year",
  treat = 3, trperiod = 1989, xperiod = 1980:1988
)
dim(spec$X0)

}
