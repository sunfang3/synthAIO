% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_plot.R
\name{autoplot.scm_fit}
\alias{autoplot.scm_fit}
\alias{plot.scm_fit}
\title{Plot an \code{scm_fit}}
\usage{
\method{autoplot}{scm_fit}(
  object,
  type = c("path", "effect", "v", "w", "gaps", "mspe", "pvalue", "loo"),
  ...
)

\method{plot}{scm_fit}(x, ...)
}
\arguments{
\item{object, x}{An \code{scm_fit}.}

\item{type}{Plot kind. Default \code{"path"}.}

\item{...}{Passed from \code{plot()} to \code{\link[ggplot2:autoplot]{autoplot()}}.}
}
\value{
A ggplot. \code{plot()} prints it and returns it invisibly.
}
\description{
ggplot2 charts for treated versus synthetic paths, the gap series,
predictor and donor weights, in-space placebos, and leave-one-out
bands. Does not refit.
}
\details{
\code{type} is one of \code{"path"}, \code{"effect"}, \code{"v"}, \code{"w"}, \code{"gaps"},
\code{"mspe"}, \code{"pvalue"}, or \code{"loo"}. Types \code{"gaps"}, \code{"mspe"}, and
\code{"pvalue"} need \verb{$placebo_space}. Type \code{"loo"} needs \verb{$loo}.
}
\examples{
fit <- scm(
  cigsale ~ lnincome + cigsale(1988),
  data = smoking, unit = "state", time = "year",
  treat = 3, trperiod = 1989, xperiod = 1980:1988
)
ggplot2::autoplot(fit, type = "path")

}
