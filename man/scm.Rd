% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scm.R
\name{scm}
\alias{scm}
\title{Estimate a synthetic control and optional diagnostics}
\usage{
scm(
  formula,
  data,
  unit,
  time,
  treat,
  trperiod,
  counit = NULL,
  xperiod = NULL,
  mspeperiod = NULL,
  preperiod = NULL,
  postperiod = NULL,
  nested = FALSE,
  allopt = FALSE,
  custom_v = NULL,
  placebo = NULL,
  loo = FALSE,
  margin = 0.05,
  maxiter = 1000,
  sigf = 7,
  bound = 10,
  start_seed = 1
)
}
\arguments{
\item{formula}{A formula. The left-hand side is the outcome. Right-hand
side terms are bare covariate names or \code{var(times)} selectors.}

\item{data}{A long panel data frame.}

\item{unit}{Column name of the unit id (string or unquoted name).}

\item{time}{Column name of the time index (string or unquoted name).}

\item{treat}{Treated unit id, matching \code{data[[unit]]}.}

\item{trperiod}{Treatment-period time point.}

\item{counit}{Optional vector of donor unit ids. The treated unit, if
listed, is dropped with a warning.}

\item{xperiod}{Times used to average bare covariates. Default: all times
\verb{< trperiod}.}

\item{mspeperiod}{Times used for the MSPE / nested objective. Default:
all times \verb{< trperiod}, independent of \code{xperiod}.}

\item{preperiod}{Pre-treatment window (RMSE, \(R^2\)). Default: all times
\verb{< trperiod}.}

\item{postperiod}{Post-treatment window (ATT, post MSPE). Default: all
times \verb{>= trperiod}.}

\item{nested}{Use nested MSPE minimization for \(V\).}

\item{allopt}{Multi-start nested optimization. Implies \code{nested}.}

\item{custom_v}{Length-\(K\) non-negative predictor weights.}

\item{placebo}{\code{NULL} or a list with \code{unit}, \code{period}, and/or \code{cut}.}

\item{loo}{If \code{TRUE}, attach leave-one-out donor robustness.}

\item{margin}{Extra primal-feasibility slack for the inner QP.}

\item{maxiter}{Inner QP iteration cap (also outer BFGS \code{maxit}).}

\item{sigf}{Solver significance digits; also the LOO weight threshold.}

\item{bound}{Upper bound on each donor weight.}

\item{start_seed}{RNG seed for the jittered allopt start.}
}
\value{
An object of class \code{scm_fit}. Optional slots \verb{$placebo_time},
\verb{$placebo_space}, and \verb{$loo} are attached when requested.
}
\description{
Builds an \code{\link[=sc_spec]{sc_spec()}}, fits it with \code{\link[=sc_fit]{sc_fit()}}, then optionally
attaches in-time / in-space placebos and leave-one-out robustness
to the same \code{scm_fit} object. Contains no estimation math of its
own.
}
\details{
Method flags: \code{allopt = TRUE} selects \code{"allopt"} and implies nested.
Otherwise \code{nested = TRUE} selects \code{"nested"}. Otherwise a non-\code{NULL}
\code{custom_v} selects \code{"custom"}. Otherwise \code{"regression"}.

\code{placebo} is \code{NULL} or a list with optional \code{unit} (\code{TRUE} or donor
ids), \code{period} (fake treatment time), and \code{cut} (pre-MSPE filter).
\code{placebo = list()} runs no placebo. If both \code{unit} and \code{period} are
set, in-time is run first, then in-space on the shifted spec.
}
\examples{
fit <- scm(
  cigsale ~ lnincome + cigsale(1988),
  data = smoking, unit = "state", time = "year",
  treat = 3, trperiod = 1989, xperiod = 1980:1988
)
fit$att

}
